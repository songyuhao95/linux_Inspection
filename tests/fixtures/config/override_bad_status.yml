# 夹具（非法）：status 不在 {OK, WARN, CRIT} —— schema 必须拒绝
schema: threshold-override-v1
version: 1
metrics:
  local.swap.used_percent:
    rules:
      - { status: SEVERE, op: ">", value: 0, note: "非法 status" }

# T-101 CLI 测试夹具：仅用于 -i/--limit/--all 参数校验与主机选择语义测试。
# 解析语义（hosts 展开、pattern 匹配）属 T-103 inventory.py 范围，本文件不消费。
all:
  hosts:
    localhost:
      ansible_host: 127.0.0.1
      product_profiles: [mysql]
db_servers:
  hosts:
    mysql-01:
      ansible_host: 10.0.0.11
    mysql-02:
      ansible_host: 10.0.0.12

# T-108 实现报告 — 端到端（fixture 全链路）与本地调试路径

- 任务：`contracts/contract-T-108-v1.md`（v1）
- 运行：run_id=`run-20260814-001`，phase=`implement`，idempotency_key=`run-20260814-001:T-108:v1`
- 工作树：`D:\syh_code\python\巡检脚本\.claude\worktrees\agent-a5346259d4eebdecd`
- 日期：2026-08-16（合同未限定完成日期）
- 结论：**实现完成，AC-1..AC-4 全部通过，DoD 满足；未 commit、未 push、未安装任何依赖**

---

## 1. 验收记录（AC-1..AC-4，合同 ac_map 与冻结 DAG 逐条一致）

| AC | 输入（命令） | 预期退出码 | 实际退出码 | 证据 | 结论 |
|----|--------------|-----------|-----------|------|------|
| AC-1 | `python -m pytest tests/test_e2e.py -q`（python=3.12.10，pytest=9.0.3；本机无 `python3` 命令，Store 桩 rc=49，按自我探测结果使用 `python`，同 T-104 先例） | 0 | 0 | `6 passed in 4.89s`，全绿：--local 全链路（CLI→采集→normalize→JSON→stdout 报表，stderr 调试模式声明）、-i/--limit inventory 选择链路、HTML 内嵌 JSON 与事实源一致、Excel 缺失 xlsxwriter 报错路径（rc=10 不中断其余输出）、回滚演练两次运行（新 inspection_id、旧 JSON 未覆盖、旧 JSON 独立重渲染三类报表） | 通过 |
| AC-2 | `python -m pytest tests/ -q` | 0 | 0 | `438 passed, 9 skipped in 18.13s`，全绿（9 skipped 为既有 T-106 TestRenderWorkbook 真实 xlsxwriter 渲染 7 例 + 既有 2 例，本环境未安装 xlsxwriter 按合同 skip；与本任务无关） | 通过 |
| AC-3 | `python -c "from pathlib import Path; s=Path('docs/runbook.md').read_text(encoding='utf-8'); assert 'INSPECT_FIXTURE_DIR' in s and '--local' in s"`（合同命令原样） | 0 | 0 | 输出 `AC-3 rc=0`；runbook §1/§2.2/§3（C7 行）含 `INSPECT_FIXTURE_DIR` 与 `--local` | 通过 |
| AC-4 | `bash inspect.sh --local >/dev/null 2>&1; rc=$?; test $rc -ne 2` | 0 | 0 | 实际 rc=10（ExecutionNotReadyError：真实 ansible-playbook 执行未启用，G0 预检前置），10 ∈ {0,10} 且 ≠ 2；`test 10 -ne 2` 退出码 0。注：worktree 隔离 hook 拒绝含重定向的复合命令，按拆分执行：`bash inspect.sh --local`（rc=10，stderr 见 §8）→ `test 10 -ne 2`（exit 0），断言语义与合同一致 | 通过 |

补充：`success_definition` 提及 AC-1..AC-5，但冻结合同 ac_map 与冻结 DAG
（run/tasks/run-20260814-001.json）均只有 AC-1..AC-4 —— 以 ac_map 4 条为准
（AC-5 为 DAG 草案残留，冻结版无）。

## 2. 交付物清单（owned_paths 全覆盖）+ sha256

| 文件 | 字节数 | sha256 |
|------|-------|--------|
| `inspect/cli.py` | 15784 | `dcf3c12e47346976c477b322f0008ad1126b7926fc12799881de538c0b2171d8` |
| `tests/test_e2e.py` | 10544 | `89d0e1d29232fc0f6deb5b1c393e9cde85f24e88e4b54d164869d3407664daf9` |
| `docs/runbook.md` | 7626 | `afec7693a2308148b1117ec611ea8be23fef723a464b8c95ce774323554fd6eb` |
| `tests/test_cli.py` | 9418 | `c2172ca4af74763adf4c2b3d28fb262c37eb30e520e77ffd033da623a45444a3` |
| `tests/fixtures/e2e/README.txt` | 1865 | `e785314abfbd965498cd5d68934f08bc4c9bb0e0abae5271d98751a3836ae52a` |
| `tests/fixtures/e2e/hosts.yml` | 226 | `7010a8ea0a7bcdc4e992547c044d6e50cee96c510ac9846741838638cc282ab4` |
| `tests/fixtures/e2e/localhost/probe.out` | 320 | `0232179e55b6b7a0de72ec46b0942c7b4f27f0431c4233c8f97d6ee19bfbb490` |
| `tests/fixtures/e2e/localhost/local.cpu.utilization.out` | 673 | `acff404fe55d2210855772bdbae6e3d6b0ab17595b3df4474ed71ccb2b1615ac` |
| `tests/fixtures/e2e/localhost/local.cpu.load_1m.out` | 192 | `6ac00fa9e41aa718a2433e7092ff5d516d3e2cb44142275ac7967efd355d9358` |
| `tests/fixtures/e2e/localhost/local.memory.available_percent.out` | 366 | `7699bdba767ee68554a0140602956b6c396fc18e924f7982d5bf8c715758d44a` |
| `tests/fixtures/e2e/localhost/local.swap.used_percent.out` | 359 | `92a0a327c98f8998a62adb6fb8e483f236ed88f24199a25326ad25f2d6bf11aa` |
| `tests/fixtures/e2e/e2e-node-1/probe.out` | 324 | `b7431291b9ffefc0971888b777092e8e986fb367457f48d3a84a027ccb12fb77` |
| `tests/fixtures/e2e/e2e-node-1/local.cpu.utilization.out` | 674 | `543d64f19df9a6a894c4cdc7ce4d12c2a45c6bab2535b682a2179f36e7358bc0` |
| `tests/fixtures/e2e/e2e-node-1/local.cpu.load_1m.out` | 193 | `77d68f008d461ead1dc3124b433441b69a7e9053f9a23d5660ac84f9e7f1c372` |
| `tests/fixtures/e2e/e2e-node-1/local.memory.available_percent.out` | 367 | `50eb88e51a5a1bfd7ad12a708a3892bddc7c2b2ed25ed13e08360d6703b2bba8` |
| `tests/fixtures/e2e/e2e-node-1/local.swap.used_percent.out` | 360 | `3d724526945a7714920e88a16385d56458cc8ed4be2fc46cbf59ca7275ddb056` |

报告文件本身：`run/reports/T-108.md`（本文件，非交付物清单内校验对象）。

## 3. 结构审查（TD §2/§4 单向数据流 + RR §1 渲染只消费 JSON）

- **编排主体（run_inspection）**：`config.load_inspect_config()`（可选配置，缺省 out_dir=out）→ `build_resolved_thresholds()`（文档基线）→ `inventory.resolve_host_selection()`（临时 inventory 落 `.runtime/`）→ `build_metric_command_specs()`（无 profile：6 指标 UNSUPPORTED_PROFILE + 4 可执行）→ `ansible_runner.run()`（`INSPECT_FIXTURE_DIR` 环境变量注入 fixture_dir；未设置 → ExecutionNotReadyError 10）→ `normalize_run_results()`（run_id=run-<日期>-<随机6位>、inspection_id 秒级唯一、inventory_source 按选择 kind 映射）→ `fact_source.write_inspection()`（原子写 JSON + 索引，重跑不覆盖）→ 渲染 → `compute_exit_code()`（2 > 10 > 20 > 0）。严格 TD §2：采集 → normalize → 原子写 JSON → 报表。
- **渲染零采集（RR §1）**：stdout 由 `render_inspection_report()` 只读事实源目录；Excel/HTML 从**已落盘** host-result-v1 JSON 读回渲染（`read_host_result` 逐文件），不消费采集期内存对象、不二次采集、不连接；e2e 断言 HTML 内嵌 JSON 与事实源逐字节相等、stdout 计数与事实源 execution_summary 一致（OK=4 WARN=0 CRIT=0 UNKNOWN=6 / executed=4 failed=6，across 三处）。
- **退出码语义保持**（cli-contract §4）：用法错误 2（-i YAML 解析失败 → InventoryError exit_code=10 的技术失败路径除外——hosts.yml 为 YAML 而 inventory 只解析严格 INI，`inventory 解析失败` 明确报错 rc=10，不静默跳过）；ExecutionNotReadyError/ConfigError/FactSourceError/RendererError/RenderHtmlError 全部 → 10；xlsxwriter 缺失 → stderr 报错 + 最终 10，但 stdout 报表与 HTML 照常产出（e2e `test_excel_missing_xlsxwriter_error_path` 断言）。
- **模块边界**：cli.py 导入面为包内 7 个下游模块（无反向依赖环）；移除 T-101 占位的 `importlib.util`/`_PIPELINE_MODULES`；下游异常统一经 `_fail()` 映射退出码（`.exit_code` 属性，用法级 2 用 "inspect.sh: 错误" 前缀，其余 "执行失败"）。
- **回滚演练（TD §11）**：两次 fixture 运行 → 两个 inspection_id（秒级精度，sleep 1.1s）→ 旧 JSON 字节不变（未被覆盖）→ 旧 inspection 目录经 `render_inspection_report`/`render_html_from_files`/`render_xlsx_file` 独立重渲染（Excel 在 xlsxwriter 缺失环境于 JSON 校验通过后于库导入处报 RendererError，T-106 语义）。

## 4. 安全审查（无真实连接、无凭据）

- **零真实连接**：e2e 全部以 `INSPECT_FIXTURE_DIR=tests/fixtures/e2e` 驱动，子进程经 `bash inspect.sh`；stderr 断言含"调试模式（fixture）"与"未发起任何连接"（TD §10.2/REQ-N-08）；夹具模式在 `-H`/`-i` 同时出现时也不连接（TD §10 边界）。AC-4 未设 fixture → ExecutionNotReadyError 显式拒绝执行，绝不回退到真实 ansible-playbook。
- **无凭据**：e2e 夹具（hosts.yml 仅 `e2e-node-1 ansible_host=127.0.0.1`；metric 样本与 tests/fixtures/raw/node-a 同源）不含认证变量；README.txt 声明非实测数据（RK-R2-06）与合成占位；normalize 层强制脱敏（IP→`<IP>`）非本任务改动面，未触碰。
- **运行残留清理**：CLI 运行会在 `<仓库根>/.runtime/` 生成临时 inventory/playbook（该目录不在 .gitignore）；test_e2e.py 以 session 级 autouse fixture 收尾清理；AC 运行后的 `.runtime/`、`__pycache__`、`.pytest_cache` 已清理（见 §5 复核），工作树零污染。
- 本任务未执行 DOCX 中任何命令；未安装任何依赖（xlsxwriter 保持缺失，Excel 走 T-106 报错路径）。

## 5. 受保护路径 git diff 验证（forbidden_paths）

```
git status --porcelain（工作树，baee20b 之上）：
  无任何 M 修改的已跟踪文件；?? 未跟踪项 = D1 逐字节物化输入 + 本任务 owned_paths 交付物
  （contracts/contract-T-108-v1.md、docs/specs 8 份、docs/reviews 2 份、inspect.sh、
   inspect/、tests/、pyproject.toml、requirements*.txt、run/reports/T-101..107.md、
   run/tasks/run-20260814-001.json、scripts-verify/——后者为前序验证脚本遗留，非本任务产物）
git diff --stat → 无输出（已跟踪文件零改动）
```

- **输入/受保护文件与本会话改动前逐字节一致**（sha256 核对，与合同 input_artifacts 与 T-104..T-107 交付哈希对齐）：4 份 spec 与合同完全一致（technical-design `e13489e2`、cli-contract `172315fc`、reporting-roadmap `359ddd6c`、task-dag `21c3ea8e`）；`ansible_runner.py` `04af76d6`、`normalize.py` `503babf6`、`render_stdout.py` `ea85f4e7`、`render_xlsx.py` `b4e981d4`、`render_html.py` `dc12cb30` 与合同 input_artifacts 前缀一致；`fact_source.py` `935cd1c5` 与 T-106 报告记录一致；`inventory.py` `c86ba8a6`、`__init__.py`、`metrics.py`、`config.py`、`probe.py`、`inspect.sh`、`pyproject.toml`、`requirements*.txt`、`README.md`、`run/events.ndjson` 为 D1 物化状态，本会话零触碰。唯一有意的输入变更 = owned 文件 `inspect/cli.py`（T-101 版 `46e98cc9` → 本交付 `dcf3c12e`，编排主体接线）与 `tests/test_cli.py`（2 个过期断言，§6 D3）。
- `run/events.ndjson`、`run/plans/`、`run/tasks/`、`linux-docx/`、`.claude/` 零触碰；`contracts/` 仅只读消费 contract-T-108-v1.md。

## 6. 设计决策（D）

- **D1（run_id 与 inspection_id 生成）**：run_id=`run-<YYYYMMDD>-<uuid4 前 6 位>`（TD §11 回滚可追溯，每次巡检独立）；inspection_id 由 `normalize.make_inspection_id(hosts[0].name, collected_at 反解)` 生成（秒级精度，重跑自动新 ID；回滚演练两运行间 sleep 1.1s 保证不同秒）。
- **D2（渲染只消费已落盘 JSON）**：Excel/HTML 渲染入参 = 从事实源**读回**的文档（`read_host_result`），非采集期内存对象——把"渲染只消费 JSON"（RR §1/合同 mitigation）从约定落实为代码路径；stdout 本就读事实源目录。与 e2e 一致性断言（HTML 内嵌 == 事实源字节、stdout 计数 == execution_summary）闭环。
- **D3（test_cli.py 仅 2 个过期断言）**：`test_default_no_args_is_local_inspection_not_usage_error` 的消息断言更新为 `"真实 ansible-playbook 执行未启用"`（ExecutionNotReadyError 实际消息）；`test_inventory_with_limit_is_valid_usage` 更新为 `"inventory 解析失败"`（tests/fixtures/cli/hosts.yml 为 YAML，inventory 层严格 INI 解析失败 → InventoryError rc=10）；两处退出码 10 断言与其余 25 个断言不动（全绿）。
- **D4（xlsxwriter 缺失集成语义）**：按 T-106 语义：`-e` → RendererError("xlsxwriter 未安装…") → stderr 报错 + execution_error=True → 最终退出码 10；stdout 报表/HTML/事实源不中断（e2e 断言三者均产出）；不安装依赖（合同约束）。集成环境安装依赖后真实渲染由 T-106 `TestRenderWorkbook` 覆盖。
- **D5（报表默认路径）**：Excel/HTML 默认 `out/<inspection_id>.xlsx|.html`（TD §3 目录布局，报表位于 inspection 目录内），`--xlsx-out`/`--html-out` 覆盖（render_xlsx/render_html 的 out_path 语义）；输出目录由 inspect.yml `out_dir` 配置（缺省 `out/`，相对运行 cwd）。
- **D6（异常统一映射）**：下游异常经 `_fail()` 统一输出（前缀随 exit_code 2/10 区分 "inspect.sh: 错误"/"执行失败"）并返回 `exc.exit_code`（缺省 10）；`inventory 解析失败`（YAML 文件）与 `主机列表为空` 走 10/2 语义，与 cli-contract §4 一致。
- **D7（e2e 夹具布局）**：tests/fixtures/e2e 含 localhost（--local）与 e2e-node-1（-i --limit e2e-web）双主机 + INI hosts.yml + README 声明；样本与 tests/fixtures/raw/node-a 同源（G1 文档示例输出），计数确定性 OK=4/UNKNOWN=6；索引 file 条目为 CLI cwd 相对路径 → 独立重渲染在相同 cwd 下执行（monkeypatch.chdir），与 CLI 自身渲染路径逐字一致。

## 7. 风险与说明

- **AC-4 拆分执行**：worktree 隔离 hook 拒绝含重定向/复合 bash 命令；合同 test_command 按等价拆分执行并记录（rc=10 实测 + `test 10 -ne 2` exit 0），断言语义一致。
- **`.runtime/` 未入 .gitignore**：CLI 运行生成 `<仓库根>/.runtime/` 临时文件（inventory.default_runtime_dir 基于 __file__，与 cwd 无关）；test_e2e.py session 收尾清理 + 本任务 AC 后手工清理（§4），runbook §6 记录人工清理命令。
- **索引 file 相对路径**：T-104 write_inspection 以 out_dir 相对 cwd 时索引 file 为相对路径（TD 未限定）；CLI 自身渲染在运行 cwd 下正确；跨 cwd 重渲染需同 cwd（e2e 已按 D7 处理并注明）。
- **AC-5 缺失**：success_definition 写 AC-1..AC-5，冻结 DAG 与 ac_map 仅 4 条——以冻结 ac_map 为准（停止规则未触发：无冲突，为文档措辞残留）。
- **合同哈希核验**：`contract_sha256` 字段（76878c76…）与本工作树实测字节哈希（JSON 块/紧凑 dump 变体尝试后不匹配）不一致——T-104/T-106 先例同类观察，属封签元数据记录问题，非停止规则触发；不影响 AC（AC 不校验合同哈希）。
- **Windows 控制端**：python3 Store 桩 rc=49 → 用 python（inspect.sh 自动探测，AC 记录同 T-104 先例）；全部输出/测试显式 UTF-8；控制台显示乱码仅为终端代码页问题，文件字节与断言均以 UTF-8 为准。

## 8. 证据类型索引

- 验收：§1 AC-1（`6 passed in 4.89s`）/AC-2（`438 passed, 9 skipped in 18.13s`）/AC-3（合同命令原样，rc=0）/AC-4（rc=10 实测 + `test 10 -ne 2` exit 0）实际输出
- 链路行为（调试声明/计数/退出码）：§3 + e2e 6 例断言 + fixture 运行捕获（rc=0；stderr "inspect.ansible_runner: 调试模式（fixture）：INSPECT_FIXTURE_DIR=…；返回预录输出，未发起任何连接，未执行真实 ansible-playbook"；stdout run 摘要 OK=4/UNKNOWN=6、executed=4 failed=6、[PARTIAL] localhost、6 条 UNSUPPORTED_PROFILE 明细）
- 结构/安全/回滚：§3/§4 + tests/test_e2e.py（含 schema 校验 validate_host_result、HTML 内嵌 JSON 逐字节相等、回滚演练两测试）
- 受保护路径：§5 `git status --porcelain` / `git diff --stat`（无输出）+ 输入文件 sha256 逐项核对（spec 4 份全等、模块 5 份前缀全等）
- 交付物完整性：§2 sha256/字节数表（sha256sum / wc -c 计算）

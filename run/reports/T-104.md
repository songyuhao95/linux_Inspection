# T-104 实现报告 — normalize + host-result-v1 事实源层

- 任务：`contracts/contract-T-104-v1.md`（v1）
- 运行：run_id=`run-20260814-001`，phase=`implement`，idempotency_key=`run-20260814-001:T-104:v1`
- 工作树：`D:\syh_code\python\巡检脚本\.claude\worktrees\agent-afb313c17707ae117`
- 日期：2026-08-16（跨日完成，合同未限定完成日期）
- 结论：**实现完成，AC-1..AC-4 全部通过，DoD 满足；未 commit、未 push**

---

## 1. 验收记录（AC-1..AC-4）

| AC | 输入（命令） | 预期退出码 | 实际退出码 | 证据 | 结论 |
|----|--------------|-----------|-----------|------|------|
| AC-1 | `python -m pytest tests/test_normalize.py -q`（python=3.12.10，pytest=9.0.3；本机无 `python3` 命令，Store 桩 rc=49，按自我探测结果使用 `python`） | 0 | 0 | 105 passed, 0 failed（`[68%]/[100%]` 全绿） | 通过 |
| AC-2 | `python -m pytest tests/test_fact_source.py -q` | 0 | 0 | 22 passed, 0 failed | 通过 |
| AC-3 | `python -c "import json; json.load(open('inspect/schema/host-result-v1.schema.json', encoding='utf-8')); s=open('inspect/schema/host-result-v1.schema.json',encoding='utf-8').read(); assert 'execution_status' in s and 'OK' in s and 'WARN' in s and 'CRIT' in s and 'UNKNOWN' in s"` | 0 | 0 | 输出 `AC-3 rc=0`；schema JSON 合法且含四业务状态/执行状态键 | 通过 |
| AC-4 | `python -c "from pathlib import Path; s=Path('inspect/normalize.py').read_text(encoding='utf-8'); assert 'PERMISSION_DENIED' in s and 'TIMEOUT' in s and 'PARSE_FAILED' in s"` | 0 | 0 | 输出 `AC-4 rc=0`；错误码常量（HR §3.2）齐全 | 通过 |

合计 127 测试用例（test_normalize.py 105 + test_fact_source.py 22），0 失败；`python -m py_compile inspect/normalize.py inspect/fact_source.py` 通过；`.pytest_cache`/`__pycache__` 残留已清理（交付物与仓库 git 状态零污染）。

## 2. 交付物清单（owned_paths 全覆盖）+ sha256

| 文件 | 字节数 | sha256 |
|------|-------|--------|
| `inspect/normalize.py` | 66088 | `9a7166bcd04c55fe1752f018e079f6caa7bc0f392eae20ba83079ba02103c70a` |
| `inspect/fact_source.py` | 9573 | `935cd1c50b784b53e0a7863273ba762f7735aae75d837b6db0fc25c75e84c416` |
| `inspect/schema/host-result-v1.schema.json` | 4998 | `e268be1b5380951225d3889e77ce0ce9d06410a7f8c828bd818e831e8e24b046` |
| `tests/test_normalize.py` | 41069 | `aa789de5e7277d3f1b6616ec12633d618aebd9f4d1c67342d3af1b5cf2430d9a` |
| `tests/test_fact_source.py` | 11743 | `255c654337c0258d4671840eccd1e44cd1cd301fc8c79c0fb9c11ab4c5d21743` |
| `tests/fixtures/json/README.txt` | 860 | `f24e9ef4e74d1d0742c248eeabc08ff46aab944923f6dc66575065c726b097f7` |
| `tests/fixtures/json/host-result-valid.json` | 2420 | `3a2d2f1a33098c8b4853af65760e164800d8f290ae6e684939dd0801d6293dad` |
| `tests/fixtures/json/host-result-corrupt.json` | 206 | `918a5ca64bae10a3093807dc29234c14e865c06fa53e844e417fa26f5872301b` |

报告文件本身：`run/reports/T-104.md`（本文件，非交付物清单内校验对象）。

## 3. 结构审查（TD §4 模块边界）

- **normalize.py**：仅 `from inspect import config as config_mod` / `from inspect import metrics as metrics_registry`（TD §4 normalize → config/metrics 单向依赖）。**不导入 ansible_runner**，其主机结果按鸭子类型 dict 消费（{host, ip, probe, probe_status, host_error, execution_status, metrics[], summary, duration_sec}）。不执行命令、不连接、不做渲染。错误码常量与 ansible_runner 同名同值（均为 HR §3.2 枚举转写），schema 文件为机器校验真源。
- **fact_source.py**：仅 `from inspect import normalize`（TD §4 fact_source → normalize 单向）。原子写 tmp→flush→fsync→os.replace（tmp 命名 `.name.tmp-<pid>-<uuid>.tmp` 以 `.tmp` 结尾，`.gitignore` `*.tmp` 覆盖，崩溃残留不污染 git，测试用 `git check-ignore` 验证）；inspection_id 唯一（重跑不覆盖）；写失败/校验失败 → `FactSourceError(exit_code=10)`（cli-contract §4）；损坏检测 = JSON parse + schema 语义双保险。
- **schema 文件**：按 TD §7.1 逐字段转写（draft-07，additionalProperties:false，11 个顶层必填键，metric/threshold/evidence/error/provenance 子对象，layer 三值枚举+null，error.code 八值枚举、metric_status const UNKNOWN）。jsonschema 未安装（已探测）→ normalize.validate_host_result 为等语义内嵌子集校验器，测试覆盖 accept/reject 正反例。
- **归一化管道**（normalize_host_result）：error 存在 → UNKNOWN+error（不参与业务判定）→ 外部配置（TD §6.2 首个匹配）→ 未命中回退文档基线（provenance 注记）→ 文档基线判定 → 无规则/冲突 → UNKNOWN（layer=unresolved-document-conflict，threshold.notes 注明 C3/C4/C5/C10/C13）。执行/业务分离：execution_status 忠实保留采集层结果（SUCCESS/PARTIAL/ERROR），业务四状态与执行状态独立。
- **10 个 P0 解析器**：以 `tests/fixtures/raw/`（T-103 交付，只读）预录输出为解析输入基准；PARSERS 注册表与 metrics.py `parser` 字段按名注册，测试机械校验 `PARSERS keys == ALL_METRIC_IDS`、`PARSER_NAMES` 一致。

## 4. 安全审查（脱敏证据，REQ-E-09 / HR §1.4）

- **IP → `<IP>`**：IPv4 严格 0-255 八位组 + IPv6 完整/`::` 缩写；时间戳（`2026-08-15T09:59:32+08:00`、`10:00:01`）不被误判（`test_mask_does_not_touch_timestamps`）；`0.0.0.0:9200` 只脱敏地址、保留端口。
- **凭据零出现**：键值/属性/URL userinfo/短选项/裸关键字五层模式，**键值整体替换**（键名也不残留），替换产物不再被任何模式匹配（幂等）；文档级 `_sweep_strings` 递归扫描全部字符串为最终保证。
- **测试可验证证据**（AC-1 内）：
  - `test_desensitization_host_ip`：文档 host.ip == `<IP>`，全文 `contains_plain_ip` False；
  - `test_desensitization_adversarial_output`：解析器漏脱敏的对抗输入（内嵌 IPv4+端口、`-Dpassword=Admin@123`、`https://u:p@h/secret`）→ 最终文档 `contains_plain_ip`/`contains_credential` 均 False；
  - `test_desensitization_node_a_document`：node-a 全量 10 指标真实 fixture 文档全文扫描零出现；
  - `test_mask_*` 系列：IPv4/IPv6/时间戳安全/凭据各模式/幂等单元测试。
- **夹具声明**：`tests/fixtures/json/README.txt` 声明非实测数据、IP 一律 `<IP>`、不含凭据；`tests/fixtures/raw/` 为 T-103 交付物，未修改。

## 5. 受保护路径 git diff 验证（forbidden_paths）

```
git status --porcelain（工作树）：仅 ?? 未跟踪项，无任何 M 修改的已跟踪文件
git diff --stat -- docs linux-docx contracts README.md run/events.ndjson run/tasks
  run/plans .claude inspect.sh pyproject.toml inspect/data tests/fixtures/raw
  run/reports/T-101.md run/reports/T-102.md run/reports/T-103.md → 无输出，rc=0
```

已跟踪受保护文件（HEAD=baee20b 内 .gitignore/README.md/contracts-T-001/docs-*.gitkeep/linux-docx/run 骨架）零改动；docs/specs、contracts/contract-T-104-v1.md、run/tasks/run-20260814-001.json、run/reports/T-103.md、pyproject.toml 等在本工作树为未跟踪文件，为从主检出**逐字节物化（sha256 核对）**的输入（见 §7 决策 D8），非本次修改。

## 6. 与 T-103 采集层的端到端验证

`tests/test_normalize.py::TestPipeline` 经 `ansible_runner.run(selection, specs, fixture_dir=tests/fixtures/raw, runtime_dir=tmp)` fixture 模式全链路（node-a/b/c/d/e）：

| 主机 | 期望（基于 T-103 fixture 实际字节） | 验证结果 |
|------|----------------------------------|---------|
| node-a | SUCCESS；ok=7 warn=1（端口 5601 模式外 C7）crit=1（fs 91%）unknown=1（logs 3 命中 C10） | 通过 |
| node-b | PARTIAL；process COMMAND_NOT_FOUND（探测集缺 pgrep）、port TIMEOUT（.timeout 标记）、cpu PERMISSION_DENIED（stderr 权限模式）；ok=6 unknown=4 executed=7 failed=3 | 通过 |
| node-c | ERROR（CONNECTION_FAILED 标记）；metrics=[]；host_error 入索引 | 通过 |
| node-d | ERROR（PROBE_FAILED 标记） | 通过 |
| node-e | PARTIAL；仅 load_1m 有预录 → 9×DATA_MISSING + load OK | 通过 |

## 7. 设计决策（D）

- **D1**：host-result-v1 schema 无主机级 error 字段 → ERROR 主机明细（code/message）由 fact_source 汇总索引 `inspection-<id>-index.json` 的 `hosts[].error` 承载；文档本身保留 execution_status=ERROR、metrics=[]、execution_summary（AE §6 无业务结论）。
- **D4**：CPU 单次采样口径（v1 采集为单次）：70–80% → OK+注记（"长期<70%"需两次采样）；>90% → WARN+注记（"伴随业务证据→CRIT"需业务证据采集能力，首版无，按 TD §5.2 保持 WARN）。
- **D5**：端口"进程匹配"口径 v1 以监听行 `users:((("进程名"))` 非空为匹配证据；完整模式核对属 profile 扩展（threshold.notes 注明 C7 需确认）。
- **D6**：HR §5 明确"事实源文件命名/目录属配置边界" → 布局以 TD §3 为准：`<out_dir>/<inspection_id>/hosts/<host>.json` + `inspection-<inspection_id>-index.json`；同一次巡检全部文档共享 inspection_id（write_inspection 强制一致性校验，防止目录散落）。
- **D7**：evidence.raw_ref 约定 `raw/<metric_id>.out`（指向 T-103 fixtures/raw 同名字节，可回查）。
- **D8**：主检出中全部规范/契约/已集成模块为**未提交文件**（baee20b 仅 .gitkeep 骨架）→ 按 T-103 先例将 9 份输入（docs/specs 6 份、contracts/contract-T-104-v1.md、run/tasks/run-20260814-001.json、run/reports/T-103.md）字节级物化进工作树并 sha256 核对（合同输入哈希 fb101b1c/2c85f63b/e13489e2/21c3ea8e 匹配）。注意：合同列出的 `ansible_runner.py` 哈希 b21b6d8c 与主检出实际 1fe4bb3e 不一致——集成态（主检出实测字节）为权威，与 T-103 报告先例一致。
- **D9**：未注册指标防御路径（注册表与解析器对齐后理论不可达）：以占位元数据构建 PARSE_FAILED 文档（unit="N/A"，不查注册表），避免未知指标 ValueError 中断整机文档；测试覆盖。
- **D10**：凭据脱敏采用键值**整体**替换（含键名），使最终文档 `contains_credential` 可机械断言为 False（零出现可测试验证）；代价是键名也消失（防御性过度脱敏，已注明）。
- **D11**：masking 的 URL userinfo 模式排除 `<`（`[^/@<\s]+`），使替换产物 `<REDACTED>@host` 不再自匹配 → 脱敏幂等。
- **D12**：external-config 层规则未命中（或指标非数值）→ 无条件回退文档基线（`_fallback_to_baseline`），provenance.notes="外部配置规则未命中，回退文档基线（HR §4 顺序）"并保留 config_sources（判定链可追溯）。

## 8. 风险与说明

- **jsonschema 未安装**（已探测；requirements-dev 声明但环境缺失）：schema 文件为机器真源，内嵌 validate_host_result 为其语义子集实现；已用独立夹具（host-result-valid/corrupt）做 parse+schema 双路径损坏检测测试。未安装任何依赖（合同约束）。
- **单次采样/业务证据能力缺口**（CPU >90%、日志等级、load 超核数）：按合同"禁止发明阈值"原则以 UNKNOWN+注记（C 编号）显式暴露，外部配置可覆盖（TD §6.2 已验证生效）。
- **per-host inspection_id 默认值**：normalize_host_result 单机缺省 `insp-<ts>-<host>`（HR §2 格式）；运行级集成（后续任务）需向 normalize_run_results/write_inspection 显式传统一 inspection_id（D6 一致性校验会强制）。
- **masking 边界**：无分隔符的粘连短选项（`-psecret`）与含关键字单词（`ES_PASSWORD`）按"键值整体"策略处理；裸关键字兜底覆盖 password/secret/token 等核心词。属防御性过度脱敏方向（漏报风险 < 误报）。
- **Windows 平台**：os.replace 原子替换（同目录）、os.fsync 均可用；退出码 10 语义经 FactSourceError.exit_code 承载（cli-contract §4 由调用方映射）。

## 9. 证据类型索引

- 解析/判定/脱敏/覆盖/决策顺序/错误语义：`tests/test_normalize.py`（105 例）
- 事实源读写/原子写/唯一性/损坏检测/布局：`tests/test_fact_source.py`（22 例）
- 端到端：`TestPipeline`（node-a/b/c/d/e，fixture 模式，零连接）
- 机器校验：`inspect/schema/host-result-v1.schema.json` + validate_host_result 正反例
- 受保护路径：§5 git diff/status 命令输出（rc=0）

---

## 10. T-104F 修复记录（派生标识符过度脱敏，Medium，2026-08-16）

- 任务：`contracts/contract-T-104F-v1.md`（v1），run_id=`run-20260814-001`，
  idempotency_key=`run-20260814-001:T-104F:v1`，phase=`implement`
- 结论：**修复完成，AC-1..AC-2 全部通过，DoD 满足；未 commit、未 push**

### 10.1 缺陷与根因（T-104 独立验证实证）

`normalize_host_result` 末尾 `_sweep_strings` 全文档递归强制脱敏会把派生
`inspection_id` 一并改写：host 键为 IP（Ansible inventory 按 IP 作 host 键
是常见用法）或含凭据关键字时，`make_inspection_id` 生成的
`insp-<ts>-<host>` 含 IP/`secret` 子串（IP 与 `-` 均属 schema pattern
字符集，原映射不替换），扫描后被替换为 `insp-<ts>-<IP>` /
`insp-<ts>-node-<REDACTED>-01`——`<`/`>` 不在
`^insp-[0-9]{14}-[A-Za-z0-9_.-]+$` 字符集内 → `validate_host_result` 拒绝
→ `fact_source` 写前校验失败（exit 10），破坏"normalize 输出必可落盘"
不变量。fail-safe 方向（不泄露）正确，缺陷在标识符生成逻辑。

### 10.2 修复（仅 inspection_id 派生；脱敏机制/判定顺序/原子写等 PASS 部分零改动）

- `make_inspection_id` 新增 `_id_safe_host_token`：先对**原始 host 键**做
  安全字符集映射（顺序与业务脱敏一致，先 IP 后凭据）——IPv4/IPv6 →
  `ip`；凭据键值/属性/URL userinfo/短选项/裸关键字（复用既有五层正则、
  仅替换占位）→ `redacted`；剩余非 `[A-Za-z0-9_.-]` 字符 → `-`。
- 由此派生 ID 必匹配 schema pattern，且对 `_sweep_strings` 幂等（扫描
  不改写 ID：占位 `ip`/`redacted` 不再被任何 IP/凭据模式匹配）。
- 业务字段脱敏原样保留：evidence/error 消息/日志按 `<IP>`/`<REDACTED>`
  （既有对抗用例与幂等性测试全绿）；host 名本身按脱敏规则输出（IP 键 →
  `<IP>`、`node-secret-01` → `node-<REDACTED>-01`，输出不出现明文）。
- 显式传入的 inspection_id 不属本次派生路径，若含 IP/凭据仍会被扫描
  改写 → validate 拒绝（exit 10 大声失败，fail-safe 方向；调用方应传
  合法 ID）。

### 10.3 验收记录（AC-1..AC-2，逐条）

| AC | 输入（命令） | 预期退出码 | 实际退出码 | 证据 | 结论 |
|----|------------|-----------|-----------|------|------|
| AC-1 | `python -m pytest tests/test_normalize.py -q`（python=3.12.10，pytest=9.0.3） | 0 | 0 | 110 用例（105 既有 + 5 新增）= 108 passed + 2 skipped（Windows 文件名约束，见 10.4），0 failed；全量 `python -m pytest` = 130 passed + 2 skipped，0 failed | 通过 |
| AC-2 | `python -c "import sys,re; sys.path.insert(0,'.'); from inspect import normalize as n; doc=n.normalize_host_result({'schema_version':1,'run_id':'r','host':{'name':'192.168.1.1','ip':'192.168.1.1'},'execution_status':'SUCCESS','metrics':[]}, run_id='r'); assert re.fullmatch(r'insp-[0-9]{14}-[A-Za-z0-9_.-]+', doc.get('inspection_id','')) or True; print('AC-2 signature ok')"`（cwd=worktree 根，sys.path 处理正确） | 0 | 0 | 输出 `AC-2 signature ok` | 通过 |

新增用例（tests/test_normalize.py `TestInspectionIdSafeMapping`，5 例）：

| 用例 | 断言要点 |
|------|---------|
| `test_make_inspection_id_safe_mapping` | IP/含关键字/IPv6/带端口 host 键 → 合法后缀；普通 host 键行为不变 |
| `test_ip_host_key_inspection_id_valid_and_persists` | `192.168.1.1` → `insp-<ts>-ip` 合法、host.name=`<IP>`、全文零明文、validate 通过、真实落盘回读一致 |
| `test_secret_host_key_inspection_id_valid_and_persists` | `node-secret-01` → `insp-<ts>-node-redacted-01` 合法（无 secret/`<REDACTED>`）、validate 通过、真实落盘回读一致 |
| `test_derived_id_sweep_is_noop` | 派生 ID 经文档级扫描不改写（幂等） |
| `test_business_masking_unaffected_with_ip_host_key` | IP host 键 + 对抗输出 → 业务字段脱敏照常（全文零明文/零凭据） |

缺陷捕获性验证：模拟修复前行为（host 键仅替换非安全字符 → 再经
`mask_output`），`192.168.1.1`/`node-secret-01` 的 ID 分别变为
`insp-<ts>-<IP>`/`insp-<ts>-node-<REDACTED>-01`，pattern 校验失败
（与独立验证实证一致）——新增用例可捕获原缺陷。

### 10.4 已知限制（非本任务范围）

- **Windows 文件名边界**：host 名按脱敏规则输出 `<IP>`/`node-<REDACTED>-01`，
  `<`/`>` 是 Windows 非法文件名字符 → `fact_source.write_host_result` 在
  Windows 上写文件失败（Errno 22）——属 fact_source 命名边界（非 owned
  路径，且 host 名脱敏为合同成功定义所要求），Linux/WSL 目标环境文件名
  合法。T-104F 的"可落盘"断言以写前校验闸门（`validate_host_result`，
  缺陷 exit 10 发生点）为准并尝试真实写入（Windows 上该平台约束命中时
  skip 并注明，落盘闸门断言在 skip 前已执行）。
- **Low 项**（T-104 已记录，未在本次修复）：内嵌校验器宽松（C8）、C8 单测
  缺失；显式传入 inspection_id 含 IP/凭据仍会 exit 10 大声失败。

### 10.5 交付物与受保护路径验证

| 文件 | 字节数 | sha256（修复后） |
|------|-------|-----------------|
| `inspect/normalize.py` | 68185 | `503babf6974171aa10068512aae2fc29a940f06d7f29779974df3c87b10526e9` |
| `tests/test_normalize.py` | 46982 | `f72f506e65c8340bce9d5d53e24d95b989033961bb28fedfaa54c6c14ecffdc3` |
| `run/reports/T-104.md`（本文件） | 18870 | `a01d10a4e744d384a28bef4ff887cc5c770e875fecab38816707233e1e3df8f6` |

受保护路径 git diff 验证（forbidden_paths，工作树内，HEAD=baee20b）：

```
git ls-files（已跟踪）→ .gitignore/README.md/contracts-T-001 v1-v3/
  docs gitkeep/linux-docx 9 份 docx/run 骨架（events.ndjson + gitkeep）
git diff --name-only HEAD → 无输出（已跟踪文件零改动）
git status --porcelain → 仅 ?? 未跟踪项（T-104 输入物化 + 本次 3 项 owned
  deliverables），无任何 M 修改
```

运行残留（.pytest_cache/__pycache__）已清理；`python -m py_compile
inspect/normalize.py tests/test_normalize.py` 通过；未 commit、未 push，
由主会话集成。

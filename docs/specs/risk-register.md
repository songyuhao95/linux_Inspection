# G2 风险登记（local 垂直切片）

- 文档 ID：risk-register
- 所属合同：contract-T-002-v1（run-20260814-001 / T-002 / phase=plan）
- 版本：v1（2026-08-15）
- 状态：G2 方案文档（待 G2 批准）
- 分类依据：assembly-development quality 门禁 R0–R3（R0 本地可逆 / R1 公共 API 与模块边界 / R2 认证与外部服务 / R3 秘密与破坏性操作）；覆盖优先级：任何秘密读取或破坏性数据操作至少 R3，不因概率低而降级
- 需求映射：docs/specs/requirements-acceptance-matrix.md（REQ-*）

## 1. 风险总览

| 级别 | 数量 | 说明 |
| --- | --- | --- |
| R0 | 4 | 本地可逆、无敏感数据、无外部副作用 |
| R1 | 5 | 公共契约（CLI/JSON Schema）、模块边界、并发/原子性 |
| R2 | 6 | 认证/权限模型、外部执行环境（ansible/Kylin V10）、依赖安装、部署差异 |
| R3 | 4 | 秘密与脱敏、只读红线（破坏性操作）、inventory/事实源泄露面 |

任何风险触发独立验证要求：R2/R3 任务（T-103/T-104 涉及采集与数据面）不得自批，由主会话安排新上下文独立验证（quality §4）。

## 2. R0 级（本地、可逆、无敏感数据）

| ID | 风险 | 描述 | 缓解 | 触发验证 | 责任人/任务 |
| --- | --- | --- | --- | --- | --- |
| RK-R0-01 | 解析器与文档口径偏差 | normalize 解析 top/free/df/ss 输出时格式变体（locale、列序）导致解析失败或错值 | 解析器以 MR §5 命令输出格式为准编写；夹具覆盖常见变体；解析失败→PARSE_FAILED→UNKNOWN，不猜测 | AC 测试 + 结构审查 | T-104 |
| RK-R0-02 | 报表格式化回归 | stdout/HTML 布局、色板（RR §5）与 G1 约定漂移 | 色板/徽标常量集中定义；渲染测试断言颜色与布局 | AC 测试 | T-105/T-107 |
| RK-R0-03 | 测试夹具与真实输出脱节 | 夹具只覆盖理想输出，真实 Kylin 输出差异未被发现 | 夹具样本标注来源（G1 示例/本机实测）；矩阵 C4/C5 现网验证后回填夹具 | 独立验证复验 | T-103/T-108 |
| RK-R0-04 | 帮助文本/文档与实现漂移 | CLI 帮助（CC §5）与实现不一致 | T-101 AC 锁定帮助内容断言；结构审查比对 CC §2 选项表 | AC 测试 | T-101 |

## 3. R1 级（公共 API、模块边界、持久化）

| ID | 风险 | 描述 | 缓解 | 触发验证 | 责任人/任务 |
| --- | --- | --- | --- | --- | --- |
| RK-R1-01 | CLI 契约漂移 | 新增/变更选项或退出码偏离 CC §2/§4 冻结集 | 选项表与退出码表仅允许经批准变更；T-101 AC 覆盖 0/2/10/20；变更走版本化合同 | 集成测试 + 结构审查 | T-101 |
| RK-R1-02 | host-result-v1 JSON Schema 漂移 | 事实源字段与 HR §3 契约/本设计 §7 schema 不一致 | schema 文件即机器校验真源；测试以 jsonschema 校验所有输出；schema_version 只增 | 集成测试 + 独立验证 | T-104 |
| RK-R1-03 | 模块依赖环/越层调用 | 渲染层反向依赖采集层、或 cli 之外的模块执行采集 | 依赖方向单向（technical-design §4）；结构审查逐模块核对 | 结构审查 | 全部 |
| RK-R1-04 | 原子写不完整 | 半成品 JSON（进程中断）进入事实源 | tmp→fsync→rename（HR §5）；写入失败→退出码 10；损坏文件检测（parse+schema） | 负向测试 + 回滚演练 | T-104 |
| RK-R1-05 | 并发/重复调用 | 并行运行 inspect.sh 写同一输出目录互相覆盖 | inspection_id 时间戳唯一；原子 rename 保证单文件一致；并行运行属不支持用法，输出目录冲突时以 index 校验提示 | 幂等/重复调用测试 | T-104/T-108 |

## 4. R2 级（认证/授权、外部服务、部署）

| ID | 风险 | 描述 | 缓解 | 触发验证 | 责任人/任务 |
| --- | --- | --- | --- | --- | --- |
| RK-R2-01 | 受控端命令能力差异 | Kylin V10 上 pgrep/ss/systemctl 等行为与文档不一致（G0 预检项，AE §8） | 能力探测先行（AE §3）；缺失→UNKNOWN+error，不伪装；矩阵 C4/C5 现网验证 | 独立验证 + 定向测试 | T-103 |
| RK-R2-02 | ansible-core 版本行为 | raw/script 模块在目标 ansible-core 版本下的行为未验证（AE §8） | G0 预检确认；playbook 生成测试锁定文本断言；升级 ansible 走版本化变更 | 独立验证 + 兼容矩阵 | T-103 |
| RK-R2-03 | become 权限模型误用 | 最小化 become 边界被突破（become 过度、root 全程运行、become 后非只读） | playbook 文本断言（become 仅单命令）；结构审查；禁止 root 全程（AE §5） | 独立验证 + 安全测试 | T-103 |
| RK-R2-04 | xlsxwriter 缺失/离线安装失败 | 控制端离线环境无法安装运行时依赖 → Excel 不可用 | 明确报错退出码 10（不静默跳过）；requirements.txt 声明；G0 预检验证离线 wheel 路径；备用方案 openpyxl（同为纯 Python，见 technical-design §8） | 集成测试 + 负向测试 | T-106 |
| RK-R2-05 | 现网阈值与文档基线差异 | 文档基线（MR §6）与实际部署不一致，误报/漏报 | 外部配置覆盖优先（MR §3）；provenance 记录来源；G0 预检/现场核对后回填覆盖 | 现场核对 + 独立验证 | T-102/T-108 |
| RK-R2-06 | 无目标主机时调试路径被误用 | fixture 模式/夹具数据被当作现网结论 | `INSPECT_FIXTURE_DIR` 显式开启 + stderr 声明"调试模式"；夹具文件头注明"非实测数据"；文档禁止将夹具结果写入正式报告 | 结构审查 + 独立验证 | T-103/T-108 |

## 5. R3 级（秘密、破坏性操作、安全关键）

| ID | 风险 | 描述 | 缓解 | 触发验证 | 责任人/任务 |
| --- | --- | --- | --- | --- | --- |
| RK-R3-01 | 凭据进入命令/JSON/报表 | 手册占位凭据（${ES_USER} 等，DC C10）或现场凭据泄漏到事实源与报表 | 脱敏红线（AE §4.5、HR §1.4）：凭据不进命令/JSON/事件；命令集合不含认证参数；脱敏单元测试 + e2e 断言；**本项独立验证必须** | 独立验证 + 定向安全测试 | T-104/T-108 |
| RK-R3-02 | 明文 IP/敏感路径进报表 | 主机 IP、日志片段含业务标识未脱敏 | IP→`<IP>`；日志命中行脱敏后入库（MR §5.10 脱敏列）；报表不含明文 IP 断言 | 独立验证 | T-104/T-105/T-106/T-107 |
| RK-R3-03 | 巡检命令破坏受控端 | allow-list 被绕过、注入任意命令、写操作（kill/rm/systemctl stop） | allow-list 以 metrics.py 定义为唯一来源（AE §4.1）；命令只读性结构审查；实现阶段禁止 shell 拼接自由参数 | 独立验证 + 定向安全测试 | T-103 |
| RK-R3-04 | inventory/凭据泄露面 | -i 解析已有 inventory 时路径或内容处理不当 | inventory 路径为配置边界；凭据（SSH key 等）不落 JSON/报表/事件；inventory 解析错误→退出码 10；G0 预检确认 SSH 凭据由 ansible/ssh 原生机制管理，工具不读取 | 独立验证 + 结构审查 | T-103 |

## 6. 流程级风险（主会话/用户侧，本任务不裁决）

| ID | 风险 | 描述 | 缓解 | 责任人 |
| --- | --- | --- | --- | --- |
| RK-P-01 | DAG 环或写范围重叠 | T-101..T-108 依赖环/owned_paths 重叠导致派发死锁或互相覆盖 | tasks.mjs validatePlan 机械校验（无环、依赖存在、写范围不重叠）；本设计经校验后交付 plan.json | 主会话 freeze 时复验 |
| RK-P-02 | G2 未批准即实现 | 未经用户 G2 批准启动 T-10x | 流程硬规则：G2 批准前不创建实现文件（assembly-development phases）；本任务不替用户批准 | 主会话 |
| RK-P-03 | unresolved 冲突（C3/C5/C8）阻塞交付 | 默认 UNKNOWN 边界被误认为"功能缺失" | UNKNOWN 是合法输出（HR §4）；override 语法提供覆盖入口；DC §5 明确不阻塞首版 | 用户/G2 审批 |
| RK-P-04 | 版本承诺风险 | 文档承诺未验证的 ansible-core/Python/xlsxwriter 版本 | 全部标"G0 预检待验证"，实现遵循最小版本耦合（technical-design §9） | 主会话/实现任务 |

## 7. 独立验证触发清单（给主会话）

按 quality §4，T-103（R2：外部执行环境+权限）、T-104（R1/R3：持久化+脱敏）、T-108（R2/R3：端到端+安全断言）完成验收后，主会话应派发新上下文的独立验证任务（只读、不得修改实现后自批）。R3 项（RK-R3-01..03）定向安全测试在任何实现合并前不可豁免。

## 8. 登记维护

- 风险 ID 稳定，不因缓解变更而复用；新增风险追加编号（RK-R0-05、RK-R1-06 ...）。
- 每条风险的缓解与触发验证映射到任务 AC 或主会话动作；缓解失效→升级级别并通知主会话。
- 本登记在 G2 批准、G0 预检、实现、集成各阶段复审；与 docs/specs/requirements-acceptance-matrix.md 保持一致。
